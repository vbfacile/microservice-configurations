spring:
  application:
    name: tea-expert
    
    cloud:
      config:
        discovery:
          enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOSTNAME:localhost}:8761/eureka/